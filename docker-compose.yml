services:

  # -------------------------
  # KAFKA
  # -------------------------
  kafka:
    image: apache/kafka:latest
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT'
      KAFKA_LISTENERS: 'INTERNAL://0.0.0.0:29092,EXTERNAL://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093'
      KAFKA_ADVERTISED_LISTENERS: 'INTERNAL://kafka:29092,EXTERNAL://localhost:9092'
      KAFKA_INTER_BROKER_LISTENER_NAME: 'INTERNAL'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@kafka:9093'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
    networks:
      - app-net

  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: kafka-ui
    ports:
      - "8080:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
    depends_on:
      - kafka
    networks:
      - app-net

  # -------------------------
  # ELASTIC STACK
  # -------------------------
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:9.4.0
    container_name: es01
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - ES_JAVA_OPTS=-Xms1g -Xmx1g
    ports:
      - "9200:9200"
    networks:
      - app-net

  kibana:
    image: docker.elastic.co/kibana/kibana:9.4.0
    container_name: kib01
    depends_on:
      - elasticsearch
    environment:
      ELASTICSEARCH_HOSTS: "http://elasticsearch:9200"
    ports:
      - "5601:5601"
    networks:
      - app-net

  # -------------------------
  # WEATHER STATIONS (PRODUCERS)
  # -------------------------
  weather-station-1:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=1
    networks:
      - app-net

  weather-station-2:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=2
    networks:
      - app-net

  weather-station-3:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=3
    networks:
      - app-net

  weather-station-4:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=4
    networks:
      - app-net

  weather-station-5:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=5
    networks:
      - app-net

  weather-station-6:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=6
    networks:
      - app-net

  weather-station-7:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=7
    networks:
      - app-net

  weather-station-8:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=8
    networks:
      - app-net

  weather-station-9:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=9
    networks:
      - app-net

  weather-station-10:
    build: ./weather-station-mock
    environment:
      - MODE=random
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=10
    networks:
      - app-net

  weather-station-11:
    build: ./weather-station-mock
    environment:
      - MODE=open-meteo
      - KAFKA_BROKER=kafka:29092
      - TOPIC_NAME=weather.telemetry.v1
      - STATION_ID=11
    networks:
      - app-net

  # -------------------------
  # CENTRAL PROCESSING 
  # -------------------------
  central-station:
    build: ./central-station
    ports:
      - "8085:8080"
    environment:
      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:29092
      - APP_TOPIC_INPUT=weather.telemetry.v1
      - APP_TOPIC_ALERTS=weather.alerts.rain.v1
      - BITCASK_DATA_PATH=/data/bitcask
      - PARQUET_DATA_PATH=/data/parquet
    volumes:
      - bitcask_data:/data/bitcask
      - parquet_data:/data/parquet
    depends_on:
      - kafka
    networks:
      - app-net

volumes:
  bitcask_data:
  parquet_data:

networks:
  app-net:
    driver: bridge