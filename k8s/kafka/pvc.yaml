apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-pvc
  namespace: weather
spec:
  # Minikube provisions this automatically using its default StorageClass.
  # No manual PersistentVolume creation needed.
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
